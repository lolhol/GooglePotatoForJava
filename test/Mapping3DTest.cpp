//
// Created by ubuntu on 3/23/24.
//

#include "Mapping3DTest.h"

/*TEST_F(Mapping3DTest, ConstructorTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_, rangeNames_);
  EXPECT_TRUE(cartographerModule.trajectory_builder != nullptr);
}

TEST_F(Mapping3DTest, AddRangeDataTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_, rangeNames_);
  cartographer::sensor::TimedPointCloudRangeData range_data;
  cartographerModule.AddRangeData(range_data);
  EXPECT_TRUE(true);
}

TEST_F(Mapping3DTest, AddImuDataTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_, rangeNames_);
  cartographer::sensor::ImuData imu_data;
  cartographerModule.AddImuData(imu_data);
  EXPECT_TRUE(true);
}

TEST_F(Mapping3DTest, AddOdometryDataTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_, rangeNames_);
  cartographer::sensor::OdometryData odometry_data;
  cartographerModule.AddOdometryData(odometry_data);
  EXPECT_TRUE(true);
}

TEST_F(Mapping3DTest, FinishTrajectoryTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_, rangeNames_);
  cartographerModule.FinishTrajectory();
  EXPECT_TRUE(true);
}

TEST_F(Mapping3DTest, FinishAllTrajectoriesTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_,
                         rangeNames_);
  cartographerModule.FinishAllTrajectories();
  EXPECT_TRUE(true);
}

TEST_F(Mapping3DTest, WriteStateTest) {
  CartographerModule3D
      cartographerModule(dir_, file_, poseUpdateCallback_, lidarScanTimeHz_, imuNames_, odomNames_, rangeNames_);
  cartographerModule.WriteState(dir_);
  EXPECT_TRUE(true);
}*/